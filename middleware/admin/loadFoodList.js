var requireOption = require('../common').requireOption;

/**
 * Loads the foodlist stored in database
 */

module.exports = function (objectrepository) {
    return function (req, res, next) {

        return next();
    };

};